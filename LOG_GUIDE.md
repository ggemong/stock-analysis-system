# 로그 확인 완전 가이드

## 📋 로그 파일이 없을 때

### 1단계: 로그 시스템 테스트

```bash
# 로그 생성 테스트 실행
python test_logging.py
```

이 스크립트가:
- ✅ 디렉토리 권한 확인
- ✅ 로그 파일 생성 테스트
- ✅ 테스트 로그 작성
- ✅ 파일 내용 확인

### 2단계: 실제 시스템 실행

```bash
# 테스트 모드 (가벼움)
python main.py --test

# 전체 분석 (실제 데이터 수집)
python main.py
```

---

## 📍 로그 파일 위치

실행하면 자동으로 생성됩니다:

```
stock-analysis-system/
├── stock_analysis.log    ⭐ 여기!
├── main.py
└── ...
```

시스템 실행 시 첫 줄에 경로가 표시됩니다:
```
📋 로그 파일 위치: /path/to/stock-analysis-system/stock_analysis.log
```

---

## 🔍 로그 확인 방법

### 방법 1: 파일로 확인 (추천)

```bash
# 전체 내용 보기
cat stock_analysis.log

# 최근 100줄 보기
tail -n 100 stock_analysis.log

# 실시간 로그 보기 (새 로그가 추가되면 자동 표시)
tail -f stock_analysis.log
```

### 방법 2: 화면 출력으로 확인

```bash
# 화면에 로그 출력하면서 실행
python main.py --test

# 화면 출력을 파일로도 저장
python main.py --test 2>&1 | tee output.log
```

### 방법 3: GitHub Actions에서 확인

1. GitHub 저장소 → **Actions** 탭
2. 최신 워크플로우 클릭
3. **Run stock analysis** 작업 클릭
4. 각 단계의 로그 확인

---

## 🎯 김치프리미엄 로그만 보기

```bash
# 김치프리미엄 관련 로그만 추출
grep "김치" stock_analysis.log

# 더 상세하게 (전후 20줄 포함)
grep -A 20 -B 5 "김치 프리미엄 수집 시작" stock_analysis.log

# 성공/실패 통계
grep "김치 프리미엄 수집 완료" stock_analysis.log

# 실패한 항목만
grep -E "실패:|❌" stock_analysis.log | grep -E "BTC|ETH|XRP|SOL|ADA"

# 환율 정보
grep "환율" stock_analysis.log
```

---

## 📊 로그 형식 이해하기

```
2025-01-31 09:00:01 - __main__ - INFO - 김치 프리미엄 수집 시작...
└─────┬─────┘   └───┬───┘   └┬┘   └──────────┬──────────┘
   시간/날짜      모듈명    레벨      메시지 내용
```

**로그 레벨:**
- `INFO` ℹ️: 일반 정보
- `WARNING` ⚠️: 경고 (기능은 작동하지만 주의 필요)
- `ERROR` ❌: 오류 (특정 기능 실패)
- `CRITICAL` 💥: 치명적 오류

---

## 🔧 디버그 모드 활성화

더 상세한 로그가 필요하면 `main.py` 수정:

```python
# 33번 줄 수정
logging.basicConfig(
    level=logging.DEBUG,  # INFO → DEBUG 변경
    ...
)
```

**디버그 모드에서 추가로 보이는 정보:**
- API 호출 URL
- 요청/응답 데이터
- 중간 계산 과정
- 자세한 에러 스택

---

## 🚨 로그 파일이 계속 생성 안 되면

### 체크리스트:

```bash
# 1. 현재 위치 확인
pwd
# 결과: /path/to/stock-analysis-system 이어야 함

# 2. 권한 확인
ls -ld .
# 결과: drwxr-xr-x (쓰기 권한 필요)

# 3. 권한 부여 (필요시)
chmod u+w .

# 4. Python 버전 확인
python --version
# 결과: Python 3.8 이상이어야 함

# 5. 의존성 확인
pip list | grep -E "requests|yfinance|tenacity"

# 6. 수동으로 파일 생성 테스트
touch test_write.log
# 오류 나면 → 권한 문제

# 7. 다른 경로로 시도
cd ~
mkdir test_logs
cd test_logs
python /path/to/stock-analysis-system/main.py --test
```

---

## 📱 텔레그램으로 로그 받기

시스템 에러는 텔레그램으로도 전송됩니다:

```python
# main.py에 이미 구현되어 있음
if self.telegram_notifier:
    error_msg = f"시스템 오류 발생:\n{str(e)}"
    self.telegram_notifier.send_error_notification(error_msg)
```

---

## 💡 빠른 문제 해결

### 증상별 해결책:

| 증상 | 원인 | 해결 |
|------|------|------|
| 로그 파일 없음 | 시스템 미실행 | `python main.py --test` |
| 로그 파일 0바이트 | 쓰기 권한 없음 | `chmod u+w .` |
| 로그 내용 비어있음 | 코드 실행 전 에러 | 화면 출력 확인 |
| GitHub에서 안 보임 | Actions 설정 오류 | `.github/workflows` 확인 |

---

## 🎓 로그 분석 예시

### 정상 케이스:
```bash
$ grep "김치 프리미엄 수집 완료" stock_analysis.log
2025-01-31 09:00:05 - INFO - 김치 프리미엄 수집 완료 - 성공: 5/5
```
→ ✅ 모든 코인 정상 수집

### 문제 케이스:
```bash
$ grep "김치 프리미엄 수집 완료" stock_analysis.log
2025-01-31 09:00:05 - INFO - 김치 프리미엄 수집 완료 - 성공: 2/5

$ grep "실패:" stock_analysis.log
2025-01-31 09:00:03 - WARNING -   ⚠️ ETH 실패: 업비트 가격 없음
2025-01-31 09:00:04 - WARNING -   ⚠️ XRP 실패: 바이낸스 가격 없음
2025-01-31 09:00:04 - WARNING -   ⚠️ SOL 실패: 업비트 가격 없음
```
→ ⚠️ 3개 코인 실패 → API 접근 문제 의심

---

## 📞 추가 도움

로그를 확인했는데도 문제를 못 찾겠다면:

1. **로그 파일 전체 내용** 백업
   ```bash
   cp stock_analysis.log stock_analysis_backup_$(date +%Y%m%d).log
   ```

2. **주요 섹션만 추출**
   ```bash
   grep -E "ERROR|WARNING|김치 프리미엄" stock_analysis.log > error_summary.log
   ```

3. **이슈 리포트 작성 시 첨부**

---

## 🎯 요약

1. **로그 파일 위치**: `stock-analysis-system/stock_analysis.log`
2. **생성 방법**: `python main.py --test` 실행
3. **확인 방법**: `tail -f stock_analysis.log`
4. **김치프리미엄**: `grep "김치" stock_analysis.log`
5. **문제 해결**: `python test_logging.py` 먼저 실행

**기억하세요**: 시스템을 실행해야 로그가 생성됩니다! 🚀
